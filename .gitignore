# environment
.venv/
__pycache__/
*.pyc

# data, checkpoints and generated artifacts (regenerable — not committed)
data/
*.pt
*.ckpt
samples.txt

# personal scratch notes (kept local)
notes.md

# NOTE: loss_curve.png IS committed on purpose (the README embeds it).
